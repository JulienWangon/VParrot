import React from 'react';
import headStyles from './headerStyles.module.css';

const Header = ({ title, slogan}) => {
  return (
    <header className={headStyles.headerClass}>
      
    </header>
  );
};

export default Header;